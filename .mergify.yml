pull_request_rules:
  - name: H1 duplicate cross-app injection readout 20260813
    conditions:
      - base = h1-dup-app-target-20260813
      - label = H1_NEVER_DUP_APP_MERGE_20260813
    actions:
      merge:
        method: merge
