commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
  dequeue:
    conditions:
      - sender-permission >= write
queue_rules:
  - name: H1 edited-comment permission freshness 20260813
    queue_conditions:
      - base = h1-edited-command-target-20260813
    merge_conditions:
      - label = H1_NEVER_EDITED_COMMAND_MERGE_20260813
    branch_protection_injection_mode: merge
    batch_size: 1
    merge_method: squash
merge_queue:
  mode: serial
  max_parallel_checks: 1
  skip_intermediate_results: false
  queue_controls_comment: false
