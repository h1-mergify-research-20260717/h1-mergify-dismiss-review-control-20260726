# H1_EXTERNAL_SOURCE_HEAD_FRESHNESS_CONTROL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor3-gif
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = H1 external source head freshness
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
