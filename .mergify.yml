# H1_FREEZE_EXISTING_PR_REEVALUATION_CONTROL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursora1-cell
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions: []
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
merge_protections:
  - name: H1 freeze existing PR reevaluation
    if:
      - base = main
    success_conditions: true
merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
