# H1_EXTERNAL_BASE_RESET_CONTROL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = H1 external base freshness
    branch_protection_injection_mode: none
    merge_method: squash
merge_queue:
  mode: isolated
  reset_on_external_merge: always
  queue_controls_comment: false
