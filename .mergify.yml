# H1_FORK_TOKEN_BOUNDARY_CONTROL_20260727
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = @github-actions/H1 fork token boundary
      - label = H1_NEVER_MERGE_FORK_TOKEN_20260727
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
pull_request_rules:
  - name: H1 auto-queue external fork token write control
    conditions:
      - title = H1 EXTERNAL FORK TOKEN WRITE CONTROL 20260727
    actions:
      queue:
        name: default
