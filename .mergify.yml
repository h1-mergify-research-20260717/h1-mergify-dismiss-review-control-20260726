commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max

queue_rules:
  - name: H1 source freshness fresh control 20260812
    queue_conditions:
      - base = h1-source-fresh-target-20260812
    merge_conditions:
      - check-success = H1 source head freshness fresh
    branch_protection_injection_mode: merge
    merge_method: squash

merge_queue:
  queue_controls_comment: false
