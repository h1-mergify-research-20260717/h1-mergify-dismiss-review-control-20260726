# H1_MANUAL_SCOPES_RECOMPOSITION_CONTROL_20260728
scopes:
  source:
    manual:
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = H1 manual scopes recomposition hold
    autoqueue: true
    branch_protection_injection_mode: none
    merge_method: squash
merge_queue:
  mode: parallel
  max_parallel_checks: 2
  queue_controls_comment: false
