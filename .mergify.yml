commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max

queue_rules:
  - name: H1 required deployment queue injection 20260813
    queue_conditions:
      - base = h1-env-recreate-target-20260813
    merge_conditions:
      - label = H1_ENV_RECREATE_HOLD_20260813
    branch_protection_injection_mode: merge
    batch_size: 1
    allow_inplace_checks: true
    merge_method: squash

merge_queue:
  mode: serial
  max_parallel_checks: 1
  skip_intermediate_results: false
  queue_controls_comment: false
