commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max

queue_rules:
  - name: H1 dequeue fresh control 20260812
    queue_conditions:
      - base = h1-dequeue-fresh-target-20260812
    merge_conditions:
      - check-success = H1 dequeue fresh
    branch_protection_injection_mode: merge
    batch_size: 1
    merge_method: squash

merge_queue:
  mode: serial
  skip_intermediate_results: false
  queue_controls_comment: false
