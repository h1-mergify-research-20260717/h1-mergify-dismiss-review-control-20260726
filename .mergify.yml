# H1_PAUSE_ENFORCEMENT_CONTROL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = H1 pause enforcement hold
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
