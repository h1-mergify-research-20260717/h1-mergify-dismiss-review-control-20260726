pull_request_rules:
  - name: H1 status latest-state freshness readout 20260813
    conditions:
      - base = h1-status-fresh-target-20260813
      - label = H1_NEVER_STATUS_FRESH_MERGE_20260813
    actions:
      merge:
        method: merge
