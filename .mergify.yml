# H1_QUEUE_APPROVAL_REVOCATION_CONTROL_20260727
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - label = H1_NEVER_MERGE_COMMAND_DEPUTY_20260727
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
pull_request_rules:
  - name: H1 trusted bot-account command deputy control
    conditions:
      - title = H1 BOT COMMENT COMMAND CONTROL 20260727
    actions:
      comment:
        message: "{{ body }}"
        bot_account: onecursor1-max
