# H1_FORK_OIDC_PROVENANCE_CONTROL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor3-gif
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = H1 fork OIDC provenance
      - label = H1_NEVER_MERGE_FORK_OIDC_20260728
    branch_protection_injection_mode: none
    merge_method: squash
merge_queue:
  queue_controls_comment: false
