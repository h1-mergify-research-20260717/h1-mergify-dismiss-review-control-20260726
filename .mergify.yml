# H1_PRIVATE_DEPENDENCY_AUTOMATIC_REQUIREMENT_CONTROL_20260728
pull_request_rules:
  - name: H1 private dependency automatic merge requirement
    conditions:
      - base = main
    actions:
      merge:
        method: squash
commands_restrictions:
  queue:
    conditions:
      - sender = onecursora1-cell
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions: []
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
