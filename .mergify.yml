# H1_BASE_RETARGET_FRESHNESS_CONTROL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max
queue_rules:
  - name: default
    queue_conditions:
      - base ~= ^(main|h1-alt-base-control-20260728)$
    merge_conditions:
      - check-success = H1 base ref freshness
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
