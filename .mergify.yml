# H1_LABEL_CONDITION_FRESHNESS_CONTROL_20260728
pull_request_rules:
  - name: H1 label condition freshness merge control
    conditions:
      - base = main
      - label = h1-merge-allow-freshness-20260728
      - check-success = H1 label condition freshness
    actions:
      merge:
        method: squash
commands_restrictions:
  queue:
    conditions:
      - sender = onecursora1-cell
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions: []
    branch_protection_injection_mode: merge
    merge_method: squash
merge_queue:
  queue_controls_comment: false
