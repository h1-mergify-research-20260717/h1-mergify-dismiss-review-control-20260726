# H1_CONFIGURATION_FRESHNESS_INITIAL_20260728
commands_restrictions:
  queue:
    conditions:
      - sender = onecursor1-max
queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - check-success = H1 configuration freshness
    autoqueue: true
    branch_protection_injection_mode: none
    merge_method: squash
merge_queue:
  queue_controls_comment: false
