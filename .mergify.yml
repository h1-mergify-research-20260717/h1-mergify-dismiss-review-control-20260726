pull_request_rules:
  - name: H1 required signatures outsider merge bypass 20260813
    conditions:
      - base = h1-required-team-gate-target-20260812
    actions:
      merge:
        method: merge
