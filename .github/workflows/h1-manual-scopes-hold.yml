name: H1 manual scopes recomposition hold

on:
  pull_request:

permissions:
  contents: read

jobs:
  hold:
    name: H1 manual scopes recomposition hold
    runs-on: ubuntu-latest
    steps:
      - name: Hold and fail safely
        run: |
          sleep 120
          echo "H1_MANUAL_SCOPES_RECOMPOSITION_HOLD_FAILED_20260728"
          exit 1

