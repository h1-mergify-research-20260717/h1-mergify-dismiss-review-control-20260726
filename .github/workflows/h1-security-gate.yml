name: H1 Security Gate Workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  security-gate:
    name: "Rule: H1 Security Gate (post_check)"
    runs-on: ubuntu-latest
    steps:
      - name: Authorized security gate source control
        run: echo "GitHub Actions source control passed"
