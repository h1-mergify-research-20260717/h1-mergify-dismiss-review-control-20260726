name: H1 label condition freshness

on:
  pull_request:

permissions:
  contents: read

jobs:
  freshness:
    name: H1 label condition freshness
    runs-on: ubuntu-latest
    steps:
      - name: Hold before success
        run: |
          sleep 120
          echo "H1_LABEL_CONDITION_FRESHNESS_PASSED_20260728"

